{
  "name": "chore-fridge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev -w @chore-fridge/web",
    "build": "npm run build -w @chore-fridge/web",
    "preview": "npm run preview -w @chore-fridge/web",
    "start": "node apps/server/src/index.js",
    "deploy": "./deploy-nas.sh",
    "test": "node --import ./tests/register.js --test tests/*.test.js"
  },
  "devDependencies": {
    "@js-ox/compiler": "^0.2.2",
    "happy-dom": "^20.14.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.13.0"
  }
}
