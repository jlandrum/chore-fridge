# Copy this file to .env and replace the placeholders with your deployment values.
# .env is ignored by Git and must never contain credentials or private keys.
NAS_HOST=user@nas.local
NAS_PATH=/path/to/chore-fridge
NAS_DOCKER_COMPOSE="docker compose"
CHORE_FRIDGE_BIND_ADDRESS=192.168.1.20
